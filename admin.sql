use fms;
create table admin( 
    first_name varchar(30),
    last_name varchar(20),
    dob date,
    gender varchar(20),
    contact_no varchar(10),
    user_id varchar(30),
    password varchar(30)
);